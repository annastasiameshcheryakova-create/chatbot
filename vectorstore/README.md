# Vector Store (Chroma)

У цій папці зберігається локальна векторна база даних Chroma.

Тут автоматично створюються:
- індекси ембеддінгів
- метадані чанків
- службові файли Chroma DB

❗ Ця папка НЕ комітиться у Git.
Вона відновлюється автоматично з `data/raw/` шляхом повторної індексації.
